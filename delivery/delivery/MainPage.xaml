<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="delivery.MainPage">

    <StackLayout >
        <ScrollView>
            <ListView x:Name="itemList" HasUnevenRows="True" ItemsSource="{Binding list}" >
                <ListView.ItemTemplate >
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <Image Source="{Binding PicPath}" WidthRequest="45" HeightRequest="60" />
                                <StackLayout>
                                    <Label Text="{Binding Name}" FontSize="18" />
                                    <Label Text="{Binding Description}" />
                                </StackLayout>
                                <Button Text="Push" Clicked ="clickPush" CommandParameter="{Binding Name}" IsEnabled="{Binding Flag}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>

        <Button Text="Cart" Clicked="goCart"/>
    </StackLayout>
</ContentPage>



